/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EnumToSelectItemsPipe } from './enum-to-select-items.pipe';

describe('Pipe: EnumToSelectItemse', () => {
  it('create an instance', () => {
    let pipe = new EnumToSelectItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
